An RPN calculator in C++. Code first parses the arithmetic expression and checks if the expression is valid. If valid, then compute the expression entered. Result of the expression is returned as a double. 
